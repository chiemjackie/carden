import React from "react";
import styled from "styled-components";

const AgainstHouse = () => {
  return <GameWrapper>Against House</GameWrapper>;
};

const GameWrapper = styled.div`
  margin: 0 20px;
  height: calc(100vh - 122px);
  /* background: red; */
`;

export default AgainstHouse;
